#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
Usage:
  ceph-request get    <uri>  [-c|--conf=<kn>] [--header=<kn>] [--download=<kn>] [--type=<kn>] [--verbose] [--pretty] [--vstyle] [-b|--bucket=<kn>]
  ceph-request put    <uri>  [-c|--conf=<kn>] [--header=<kn>] [--file=<kn>] [--content=<kn>] [--type=<kn>] [--verbose] [--pretty] [--vstyle] [-b|--bucket=<kn>]
  ceph-request post   <uri>  [-c|--conf=<kn>] [--header=<kn>] [--file=<kn>] [--content=<kn>] [--type=<kn>] [--verbose] [--pretty] [--vstyle] [-b|--bucket=<kn>]
  ceph-request head   <uri>  [-c|--conf=<kn>] [--header=<kn>] [--type=<kn>] [--verbose] [--pretty] [--vstyle] [-b|--bucket=<kn>]
  ceph-request delete <uri>  [-c|--conf=<kn>] [--type=<kn>] [--verbose] [--pretty] [--vstyle] [-b|--bucket=<kn>]
  ceph-request (-h | --help)
  ceph-request --version

Options:
  -h --help         Show help.
  --version         Show version.
  -c --conf=<kn>    Specify config file [default: ~/ceph-request.cfg].
  --header=<kn>     Set HTTP request header.
  --download=<kn>   Download to local.
  --file=<kn>       File to upload.
  --content=<kn>    Content to upload.
  --type=<kn>       Type s3 or swift.
  --verbose         More information of request.
  --pretty          Pretty print output of json or xml
  --vstyle          Use Vitual Host Style Rather than Path Style
  -b --bucket=<kn>  Specify bucket name when use Vitual Host Style

==EXAMPLE ceph-request.cfg==
[s3]
host = 192.168.10.100
port = 80
access_key = your_access_key
secret_key = your_secret_key
[swift]
host = 192.168.10.100
port = 80
subuser = test:swift
secret_key = gA2BrFTKD3GyDd9b3FIOxDih0PRZBFda13f92GxP

"""

from os.path import expanduser

from docopt import docopt

from http_s3_requests import s3_head, s3_delete, s3_get, s3_post, s3_put
from http_swift_requests import swift_head, swift_delete, swift_get, swift_post, swift_put


def get_configure(config_file,type='s3'):
    ceph_request_config = {}
    try:
        import ConfigParser
        config = ConfigParser.RawConfigParser()
        config.read(config_file)
        if type == 's3':
            ceph_request_config['s3_host'] = config.get('s3', 'host')
            ceph_request_config['s3_port'] = config.get('s3', 'port')
            ceph_request_config['s3_access_key'] = config.get('s3', 'access_key')
            ceph_request_config['s3_secret_key'] = config.get('s3', 'secret_key')
        elif type == 'swift':
            ceph_request_config['swift_host'] = config.get('swift', 'host')
            ceph_request_config['swift_port'] = config.get('swift', 'port')
            ceph_request_config['swift_subuser'] = config.get('swift', 'subuser')
            ceph_request_config['swift_secret_key'] = config.get('swift', 'secret_key')
        else:
            pass
    except ConfigParser.NoSectionError, err:
        print 'Error Config File:', err
    return ceph_request_config

def main():
    args = docopt(__doc__, version='ceph-request 2.0.5')
    _configure_file =  expanduser("~") + '/ceph-request.cfg' if args['--conf'] is None else args['--conf'][0]
    _cmd = args['<uri>']
    _headers = args['--header']
    _file = args['--file']
    _content = args['--content']
    _show_dump = args['--verbose']
    _down_load_file = args['--download']
    _type = args['--type']
    _pretty = args['--pretty']
    _vstyle = args['--vstyle']
    _bucket = args['--bucket'][0] if _vstyle else None

    if args['get']:
        if str(_type).lower() == 'swift':
            ceph_request_config = get_configure(_configure_file,type='swift')
            swift_get(
                host=ceph_request_config['swift_host'],
                port=ceph_request_config['swift_port'],
                cmd=_cmd,
                subuser=ceph_request_config['swift_subuser'],
                secret_key=ceph_request_config['swift_secret_key'],
                headers=_headers,
                show_dump=_show_dump,
                download_file=_down_load_file,
            )
        else:
            ceph_request_config = get_configure(_configure_file, type='s3')
            s3_get(
                host=ceph_request_config['s3_host'],
                port=ceph_request_config['s3_port'],
                cmd=_cmd,
                access_key=ceph_request_config['s3_access_key'],
                secret_key=ceph_request_config['s3_secret_key'],
                headers=_headers,
                show_dump=_show_dump,
                download_file=_down_load_file,
                pretty=_pretty,
                vstyle=_vstyle,
                bucket=_bucket
            )
    if args['post']:
        if str(_type).lower() == 'swift':
            ceph_request_config = get_configure(_configure_file, type='swift')
            swift_post(
                host=ceph_request_config['swift_host'],
                port=ceph_request_config['swift_port'],
                cmd=_cmd,
                subuser=ceph_request_config['swift_subuser'],
                secret_key=ceph_request_config['swift_secret_key'],
                headers=_headers,
                file=_file,
                content=_content,
                show_dump=_show_dump,
            )
        else:
            ceph_request_config = get_configure(_configure_file, type='s3')
            s3_post(
                host=ceph_request_config['s3_host'],
                port=ceph_request_config['s3_port'],
                cmd=_cmd,
                access_key=ceph_request_config['s3_access_key'],
                secret_key=ceph_request_config['s3_secret_key'],
                headers=_headers,
                file=_file,
                content=_content,
                show_dump=_show_dump,
                pretty=_pretty,
                vstyle=_vstyle,
                bucket=_bucket
            )
    if args['put']:
        if str(_type).lower() == 'swift':
            ceph_request_config = get_configure(_configure_file, type='swift')
            swift_put(
                host=ceph_request_config['swift_host'],
                port=ceph_request_config['swift_port'],
                cmd=_cmd,
                subuser=ceph_request_config['swift_subuser'],
                secret_key=ceph_request_config['swift_secret_key'],
                headers=_headers,
                file=_file,
                content=_content,
                show_dump=_show_dump,
            )
        else:
            ceph_request_config = get_configure(_configure_file, type='s3')
            s3_put(
                host=ceph_request_config['s3_host'],
                port=ceph_request_config['s3_port'],
                cmd=_cmd,
                access_key=ceph_request_config['s3_access_key'],
                secret_key=ceph_request_config['s3_secret_key'],
                headers=_headers,
                file=_file,
                content=_content,
                show_dump=_show_dump,
                pretty=_pretty,
                vstyle=_vstyle,
                bucket=_bucket
            )
    if args['delete']:
        if str(_type).lower() == 'swift':
            ceph_request_config = get_configure(_configure_file, type='swift')
            swift_delete(
                host=ceph_request_config['swift_host'],
                port=ceph_request_config['swift_port'],
                cmd=_cmd,
                subuser=ceph_request_config['swift_subuser'],
                secret_key=ceph_request_config['swift_secret_key'],
                show_dump=_show_dump,
            )
        else:
            ceph_request_config = get_configure(_configure_file, type='s3')
            s3_delete(
                host=ceph_request_config['s3_host'],
                port=ceph_request_config['s3_port'],
                cmd=_cmd,
                access_key=ceph_request_config['s3_access_key'],
                secret_key=ceph_request_config['s3_secret_key'],
                show_dump=_show_dump,
                pretty=_pretty,
                vstyle=_vstyle,
                bucket=_bucket
            )
    if args['head']:
        if str(_type).lower() == 'swift':
            ceph_request_config = get_configure(_configure_file, type='swift')
            swift_head(
                host=ceph_request_config['swift_host'],
                port=ceph_request_config['swift_port'],
                cmd=_cmd,
                subuser=ceph_request_config['swift_subuser'],
                secret_key=ceph_request_config['swift_secret_key'],
                show_dump=_show_dump,
            )
        else:
            ceph_request_config = get_configure(_configure_file, type='s3')
            s3_head(
                host=ceph_request_config['s3_host'],
                port=ceph_request_config['s3_port'],
                cmd=_cmd,
                access_key=ceph_request_config['s3_access_key'],
                secret_key=ceph_request_config['s3_secret_key'],
                show_dump=_show_dump,
                pretty=_pretty,
                vstyle=_vstyle,
                bucket=_bucket
            )

if __name__ == '__main__':
    main()
